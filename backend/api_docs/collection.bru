docs {
  Each endpoint will have a field named Authorization. It can be CAREGIVER or USER or None
  * CAREGIVER means this endpoint requires CAREGIVER role
  * USER means this endpoint requires USER role
  * None means no authorization required. It is a public endpoint
  
  
  
}
