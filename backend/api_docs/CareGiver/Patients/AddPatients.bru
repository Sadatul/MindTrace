meta {
  name: AddPatients
  type: http
  seq: 1
}

post {
  url: {{host}}/v1/caregivers/patients
  body: json
  auth: bearer
}

body:json {
  {
    "patientId": "string",
    "otp": "otp"
  }
}

docs {
  **Desc**: Used to add a caregiver for patient with after otp \
  **Authorization**: CAREGIVER \
  **Response Bodies**:
  ```
  status: 201
  
  {
    "msg": "Added patient",
  }
  ```
  
  **NOTES:**
  * OTP will be verified before adding patient
}
